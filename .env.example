# Contributor CRM server-side Xano auth (Contributor-role service account)
DCP_EMAIL=
DCP_PW=

# Cron cache warmers (can remain a separate account with broader read access)
CRON_AUTH_EMAIL=
CRON_AUTH_PASSWORD=
